# US-xxx — {기능 이름}

<!-- `docs/specs/` 컴포넌트 스펙의 틀이다. 복사해서 쓴다.

     - **한 세션 크기** — `/feature` 한 번에 끝나는 크기로 자른다. 넘치면 US 를 둘로 나눈다
     - **차례가 온 US 만 그때 쓴다** — PRD §3 Must 목록을 미리 전부 스펙으로 풀지 않는다.
       앞 런의 회고가 다음 스펙을 바꾼다
     - 파일명 `US-xxx-<slug>.md` · 브랜치 `feat-us-xxx-<slug>` — 브랜치를 만들고 이 파일을 먼저 커밋한다
     - 새 세션에서 `/feature` 에 **이 파일 경로만** 준다 — 이 파일이 그대로 동결 요청 원문이 된다
       (ADR-H079). 추가 조건은 요청 문장이 아니라 여기 적는다
     - 절 제목은 지우지 말고 **빈 절로 남긴다** — 칸이 사라지면 안 정했다는 사실도 같이 사라진다 -->

## 1. 목적 · 성공 기준

<!-- PRD §3 의 어느 US 인가 · 이 기능이 끝나면 사용자에게 무엇이 달라지는가 -->

## 2. 동작

<!-- 입력 · 출력 · 관측 가능한 결과. 화면이면 사용자가 보는 것, API 면 응답이 무엇인가 -->

## 3. 인수 조건

<!-- AC-1, AC-2 … 번호를 붙이고 Given / When / Then 으로 적는다.
     **기계가 판정할 수 있게** — 테스트 하나로 참·거짓이 갈리는 문장이어야 한다.
     AC 는 여기에만 둔다. PRD 에 다시 적으면 두 곳이 갈라진다 -->

## 4. 엣지 케이스

<!-- 실패(시스템이 못 함)와 결과 없음(데이터가 없음)을 가른다.
     오류 이름은 `docs/API_SPEC.md` 에러 응답 규약의 것을 쓴다 -->

## 5. 제약

<!-- TRD 공통 밖의, 이 기능에만 있는 제약만 (성능 상한 · 권한 · 데이터 보존 등) -->

## 6. 범위 밖

<!-- 이번에 하지 않는 것. 비슷해 보여도 다른 US 의 몫인 것 -->

## 7. 인터페이스

<!-- API 는 `docs/API_SPEC.md` 의 항목을 **가리키기만** 한다. 새 엔드포인트면 같은 커밋에서
     API_SPEC 을 먼저 고친다. 코드 시그니처는 적지 않는다 — 03 계약이 확정한다 -->

## 8. 열린 질문

<!-- **아직 모르는 것을 모른다고 적는다.** 빈 절로 두는 것보다 낫다 -->
